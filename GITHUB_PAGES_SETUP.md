# Настройка GitHub Pages - Пошаговая инструкция

## ⚠️ Важно: GitHub Actions не запустится автоматически!

Workflow файл `.github/workflows/deploy.yml` существует и правильно настроен, НО GitHub Actions не запустится, пока вы не включите GitHub Pages в настройках репозитория.

## 📋 Пошаговая инструкция

### Шаг 1: Откройте настройки репозитория
1. Перейдите на GitHub: https://github.com/Wifimaxme/CloudeOSLanding
2. Нажмите на вкладку **Settings** (Настройки)

### Шаг 2: Найдите раздел Pages
1. В левом меню найдите раздел **Pages** (обычно в разделе "Code and automation")
2. Кликните на **Pages**

### Шаг 3: Настройте Source
В разделе **Build and deployment**:

1. **Source:** Выберите **"GitHub Actions"** из выпадающего списка
   - НЕ выбирайте "Deploy from a branch"!
   - Именно **"GitHub Actions"** - это критично!

2. Нажмите **Save** (если есть кнопка)

### Шаг 4: Проверьте, что настройки применились
После выбора "GitHub Actions" вы должны увидеть:
- Текст "Your site is ready to be published at https://wifimaxme.github.io/CloudeOSLanding/"
- Или "Your GitHub Pages site is currently being built from the gh-pages branch"

### Шаг 5: Триггерните workflow
Теперь нужно сделать любой коммит, чтобы запустить workflow:

```bash
# Вариант 1: Пустой коммит
git commit --allow-empty -m "trigger: запуск GitHub Actions"
git push origin main

# Вариант 2: Любое изменение
echo "# Update" >> README.md
git add README.md
git commit -m "trigger: запуск GitHub Actions"
git push origin main
```

### Шаг 6: Проверьте запуск workflow
1. Перейдите в раздел **Actions**: https://github.com/Wifimaxme/CloudeOSLanding/actions
2. Вы должны увидеть запущенный workflow "Deploy to GitHub Pages"
3. Кликните на него, чтобы увидеть прогресс

### Шаг 7: Дождитесь завершения
- Workflow занимает 2-3 минуты
- Зеленая галочка ✅ = успешно
- Красный крестик ❌ = ошибка (кликните, чтобы увидеть логи)

### Шаг 8: Откройте сайт
После успешного завершения сайт будет доступен по адресу:
**https://wifimaxme.github.io/CloudeOSLanding/**

---

## 🔍 Проверка: Почему Actions не запускается?

### Причина 1: GitHub Pages не настроен
**Симптом:** В разделе Actions пусто, workflow не появляется

**Решение:** Выполните Шаги 1-3 выше

### Причина 2: Source выбран неправильно
**Симптом:** В Actions ничего нет, но Pages настроен

**Проверка:** 
1. Settings → Pages
2. Source должен быть **"GitHub Actions"**, НЕ "Deploy from a branch"

**Решение:** Измените Source на "GitHub Actions"

### Причина 3: Workflow файл в неправильном месте
**Симптом:** Actions не видит workflow

**Проверка:**
```bash
ls -la .github/workflows/deploy.yml
# Должен показать файл
```

**Решение:** Файл должен быть точно по пути `.github/workflows/deploy.yml`

### Причина 4: Синтаксическая ошибка в YAML
**Симптом:** Workflow не парсится

**Проверка:** 
1. Откройте `.github/workflows/deploy.yml`
2. Проверьте отступы (должны быть пробелы, не табы)
3. Используйте YAML validator: https://www.yamllint.com/

### Причина 5: Нет прав на GitHub Actions
**Симптом:** "Workflows aren't being run on this repository"

**Решение:**
1. Settings → Actions → General
2. Убедитесь, что выбрано "Allow all actions and reusable workflows"
3. Сохраните изменения

---

## ✅ Быстрая проверка настроек

Выполните эту команду, чтобы проверить, что всё на месте:

```bash
cd /Users/dr/CloudeOSLanding

echo "Проверка файлов..."
test -f .github/workflows/deploy.yml && echo "✅ deploy.yml существует" || echo "❌ deploy.yml отсутствует"
test -f public/404.html && echo "✅ 404.html существует" || echo "❌ 404.html отсутствует"

echo -e "\nПроверка vite.config.ts..."
grep "base:" vite.config.ts && echo "✅ base path настроен" || echo "❌ base path отсутствует"

echo -e "\nПроверка index.html..."
grep "type=\"module\"" index.html && echo "✅ module script есть" || echo "❌ module script отсутствует"
grep "importmap" index.html && echo "❌ import map найден (нужно удалить!)" || echo "✅ import map отсутствует"

echo -e "\nПроверка сборки..."
npm run build && echo "✅ Сборка успешна" || echo "❌ Сборка провалилась"
```

---

## 📞 Если ничего не помогает

1. **Проверьте статус GitHub:** https://www.githubstatus.com/
   - Возможно, GitHub Actions временно недоступен

2. **Проверьте лимиты:**
   - Settings → Billing → Plans and usage
   - Убедитесь, что у вас есть доступные минуты Actions

3. **Пересоздайте workflow:**
   ```bash
   rm .github/workflows/deploy.yml
   # Скопируйте содержимое из DEPLOYMENT_GUIDE.md
   git add .github/workflows/deploy.yml
   git commit -m "fix: пересоздание workflow"
   git push origin main
   ```

4. **Обратитесь в поддержку GitHub:**
   - https://support.github.com/

---

## 🎯 Итоговый чеклист

- [ ] Settings → Pages → Source = "GitHub Actions"
- [ ] Файл `.github/workflows/deploy.yml` существует
- [ ] Сделан коммит после настройки Pages
- [ ] В разделе Actions появился workflow
- [ ] Workflow завершился успешно (зеленая галочка)
- [ ] Сайт открывается: https://wifimaxme.github.io/CloudeOSLanding/

Если все пункты выполнены - поздравляю, сайт работает! 🎉

